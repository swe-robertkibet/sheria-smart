# Production Environment Configuration for cPanel Deployment
# Database Configuration (Update with your cPanel MySQL details)
DATABASE_URL="mysql://your_db_user:your_db_password@localhost:3306/your_db_name"

# JWT Configuration
JWT_SECRET="your_secure_jwt_secret_key_here"

# Email Configuration (SMTP)
EMAIL_ENABLED="true"
SMTP_HOST="mail.robertkibet.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="sheria@robertkibet.com"
SMTP_PASS="B}2ukDj[a&GWIqiw"
SMTP_FROM_NAME="Sheria Smart"
SMTP_FROM_ADDRESS="sheria@robertkibet.com"

# Google OAuth Configuration (Update with production OAuth credentials)
GOOGLE_CLIENT_ID="your_production_google_client_id"
GOOGLE_CLIENT_SECRET="your_production_google_client_secret"

# Gemini AI Configuration
GEMINI_API_KEY="your_gemini_api_key"

# Document Generation Configuration
TEMPLATES_PATH="./templates"
OUTPUT_PATH="./generated"
TEMP_PATH="./temp"
DOCUMENT_OUTPUT_DIR="./generated-documents"
MAX_FILE_SIZE="52428800"  # 50MB in bytes
DEFAULT_EXPIRY_HOURS="72"  # 3 days

# Application Configuration - PRODUCTION URLS
NODE_ENV="production"
PORT="3001"
FRONTEND_URL="https://sheria.robertkibet.com"
BACKEND_URL="https://api.sheria.robertkibet.com"

# Security & Performance Configuration
MAX_JSON_SIZE="10mb"
MAX_URL_ENCODED_SIZE="10mb"

# Email Deliverability (DNS Records Setup Required)
# SPF Record: "v=spf1 include:privateemail.com ~all"
# DKIM: Configured through email provider
# DMARC: "v=DMARC1; p=quarantine; rua=mailto:dmarc@robertkibet.com"